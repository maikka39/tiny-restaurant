<nav class="bg-gray-200 navbar navbar-expand-lg navbar-light bg-light">
    <div class="max-w-7xl mx-auto px-2 sm:px-6 lg:px-8 collapse navbar-collapse" id="toggler">
        <div class="relative flex items-center justify-between h-16">
            <div class="flex-1 flex items-center justify-center sm:items-stretch sm:justify-start">
                <div class="hidden sm:block sm:ml-6">
                    <div class="flex space-x-4">
                        <a href="{{url('#')}}" class="navbar-text" aria-current="page">Tiny Restaurant</a>
                        <a href="{{url('#')}}" class="navbar-text">Nieuws</a>
                        <a href="{{url('#')}}" class="navbar-text">Agenda</a>
                        <a href="{{url('#')}}" class="navbar-text">Gemeentes</a>
                        <a href="{{url('#')}}" class="navbar-text">Contact</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="sm:hidden" id="mobile-menu">
        <div class="px-2 pt-2 pb-3 space-y-1">
            <a href="{{url('#')}}" class="navbar-text navbar-text-small" aria-current="page">Home</a>
            <a href="{{url('#')}}" class="navbar-text navbar-text-small">Nieuws</a>
            <a href="{{url('#')}}" class="navbar-text navbar-text-small">Agenda</a>
            <a href="{{url('#')}}" class="navbar-text navbar-text-small">Gemeentes</a>
            <a href="{{url('#')}}" class="navbar-text navbar-text-small">Contact</a>
        </div>
    </div>
</nav>

